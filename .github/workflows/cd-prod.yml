name: CD - Deploy Prod

on:
  pull_request:
    branches: [prod]
    types: [opened, synchronize]

permissions:
  contents: write
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}-backend

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: prod
          fetch-depth: 0

      - name: Configure git identity
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Merge PR branch into prod
        run: |
          git fetch origin ${{ github.event.pull_request.head.ref }}
          git merge --no-ff origin/${{ github.event.pull_request.head.ref }} \
            -m "Merge PR #${{ github.event.pull_request.number }} into prod (auto-deploy)"

      - name: Set short SHA
        run: echo "SHORT_SHA=$(git rev-parse --short HEAD)" >> $GITHUB_ENV

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push prod image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.prod
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:prod-${{ env.SHORT_SHA }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:prod-latest

      - name: Update prod manifest with new image tag
        run: |
          sed -i "s|image: .*backend.*|image: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:prod-${{ env.SHORT_SHA }}|" k8s/prod/deployment.yaml
          git add k8s/prod/deployment.yaml
          git commit -m "chore(prod): deploy backend prod-${{ env.SHORT_SHA }} (PR #${{ github.event.pull_request.number }})" --allow-empty
          git push origin HEAD:prod

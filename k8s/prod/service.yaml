apiVersion: v1
kind: Service
metadata:
  name: backend-prod
  namespace: chat-prod
  labels:
    app: backend
    env: prod
spec:
  type: ClusterIP
  selector:
    app: backend
    env: prod
  ports:
    - port: 5000
      targetPort: 5000

# Development image: tsx watch mode with hot reload, dev deps included.
FROM node:20-alpine AS dev
WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

EXPOSE 5000
ENV NODE_ENV=development

CMD ["npm", "run", "dev"]
